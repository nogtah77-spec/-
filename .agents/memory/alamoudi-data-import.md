---
name: Alamoudi seed data & import architecture
description: Decisions for baking property data and the Excel/CSV import in the no-backend alamoudi platform
---

# Alamoudi seed + import

The `alamoudi` artifact is UI-only (React + localStorage, no backend, user's explicit choice).

## Seed baking + migration
- Property data is baked into `src/data/seedProperties.ts` (generated by `scripts/genSeed.ts` from the user's Excel via `gen:seed`). All visitors see it.
- `DataContext` seeds new users on first load; for existing users it merges only seed properties whose `code` is missing, and writes `alamoudi_seed_version` to localStorage so it does not re-seed.
- **Why:** lets admins edit/delete baked listings without them reappearing on reload.
- **How to apply:** bumping `SEED_VERSION` re-runs the missing-by-code merge — this can resurrect listings an admin deleted, because deletions are not tracked in a ledger. Only bump the version when that trade-off is acceptable, or add a deleted-codes ledger first.

## Import (daily refresh)
- `importProperties` upserts by exact `code`. Rows with a matching code update in place; rows without a code get a generated `ALM-#####` code, so re-importing the same row without a code creates duplicates.
- **How to apply:** the daily-refresh workflow only stays duplicate-free if source rows carry stable codes. The user's Excel does (genSeed preserves codes, suffixes dup codes with `-2`).
- Parser lives in `src/lib/propertyImport.ts` (Arabic digit conversion, مليون/ألف price parsing, sheet-name→region/category, source→agentType). Accepts xlsx/xls/csv/txt/tsv.

## `source` is admin-only (code-split, not in public bundle)
- The deal `source` is private to the admin. It is NOT in `seedProperties.ts`; genSeed splits it into `src/data/seedSources.ts` (`SEED_SOURCES: code→source`).
- `seedSources.ts` must be imported ONLY from admin-only, code-split modules (currently `admin/PropertyForm.tsx`), and all `admin/*` pages are `React.lazy` in `App.tsx`. This keeps source data in the admin JS chunk, which public visitors never download.
- **Why:** no-backend SPA — the only way to keep data off a visitor's machine is to put it in a route chunk gated behind the (client-side) admin login and never import it from public code.
- **How to apply:** never import `seedSources` (or render `property.source`) from any non-admin/eagerly-loaded module, or it leaks back into the public bundle. `PropertyDetails` must never show source. Keep admin pages lazy.

## Property CODE is the customer-facing identifier (not the descriptive title)
- The property `code` (e.g. `Hh1`) is THE reference shown to customers. Display `property.code` as the prominent heading on PropertyCard (all sizes), PropertyDetails (breadcrumb + h1), and Compare header. The long descriptive `title` is NOT shown as a heading.
- Detailed info (region, specs, description) stays in the card/detail body. `title` is still kept as data and used in non-display contexts only: share metadata, image `alt`, and search matching.
- **Why:** the business identifies/quotes properties to clients by code; the descriptive title is internal clutter as a heading.
- **How to apply:** when adding any new property display surface, show the code as the title; never resurrect `property.title` as a visible heading.

## Scroll-to-top on route change
- App uses wouter. A `ScrollToTop` component (`useLocation` + `useEffect(window.scrollTo(0,0), [location])`) is mounted inside `<WouterRouter>` in `App.tsx` to fix routes opening scrolled to the bottom.
- **How to apply:** if hash/deep-link anchors are ever added, gate the reset to pathname-only so it doesn't override anchor navigation.
